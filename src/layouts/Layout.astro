---
import "../styles/globals.css";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import SEO from "../components/SEO.astro";
import { analytics as analyticsCfg } from "../config/site";
---

<html lang="nl" class="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- SEO: standaardwaarden + overschrijfbare props -->
    <SEO
      title={Astro.props.title ?? "BJORNOS — House DJ"}
      description={Astro.props.description ?? "Ik breng energie en flow naar elke set. Professionele House DJ voor clubs en festivals."}
      ogImage={Astro.props.ogImage ?? "/og.svg"}
    />

    <!-- Google Analytics 4 -->
    {analyticsCfg.gaMeasurementId && (
      <>
        <script async src={`https://www.googletagmanager.com/gtag/js?id=${analyticsCfg.gaMeasurementId}`}></script>
        <script>
          window.dataLayer = window.dataLayer || [];
          function gtag(){{dataLayer.push(arguments);}}
          gtag('js', new Date());
          gtag('config', '{analyticsCfg.gaMeasurementId}', {{ anonymize_ip: true }});
        </script>
      </>
    )}

    <!-- Favicon -->
    <link rel="icon" href="/favicon.svg" />
  </head>

  <body>
    <Navbar />

    <main id="main" class="container-px">
      <slot />
    </main>

    <Footer />

    <!-- Scroll animaties -->
    <script type="module" src="/src/scripts/reveal.ts"></script>
  </body>
</html>
